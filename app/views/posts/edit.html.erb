
<h2>Editing post </h2>
<div class="row">
  <div class='col-md-12'>
    <p style='text-align:right;'>
      created by <%= current_user.name %>, <%= time_ago_in_words(@post.created_at) %> ago
    </p>
  </div>
</div>
<%= render 'form' %>

<hr>
<%= link_to 'Show', show_post_path(teamid: @teamid), class: 'btn btn-default' %>
<%= link_to 'Back', show_path(teamid: @teamid), class: 'btn btn-default' %>
